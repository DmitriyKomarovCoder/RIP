<!DOCTYPE html>

<html>
    <head>
        <link href="{{.CSSFile}}" rel="stylesheet" type="text/css">
        <meta charset="UTF-8">
    </head>
 
    <body>
        <div class="search">
            <form action="/tenders" method="GET">
            <input type="text" name="query" placeholder="Искать здесь...">
            <button type="submit"></button>
            </form>
        </div>
        <div class="container">
        {{range .card}}
            {{template "product" .}}
        {{end}}
        </div>
    </body>
</html>