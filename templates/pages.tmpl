<!DOCTYPE html>
<html>
<head>
    <title>{{.card.Title}}</title>
    <link href="../{{.CSSFile}}" rel="stylesheet" type="text/css">
</head>
<body>
    <div class="search"><h2 id="title-text">{{.card.Title}}</h3> </div>
    <div class="pages2">
        <img id="main-img" src="../{{.card.Img}}" alt="{{.Title}}">
        <p>{{.card.Description}}</p>
        <a id="button-url" href="/tenders">Назад</a>
    </div>
</body>
</html>
